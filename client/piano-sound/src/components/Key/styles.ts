import styled from "styled-components";

export const KeyText = styled.div`
    position: relative;
    color: black;
    font-weight: 600;
    font-size: 36px;
    margin-top: 240px;
    pointer-events: none;
`;
